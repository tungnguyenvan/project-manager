enum RequestField {
    // root fields
    BODY = "body",
    QUERY = "query",
    PARAMS = "params",
    FILE = "file",

    // custom fields
    URL = "url",
    LIMIT = "limit",
    PAGE = "page",
    ID = "id",
    FIRST_NAME = "first_name",
    LAST_NAME = "last_name",
    PASSWORD = "password",
    EMAIL = "email",
}

export default RequestField;